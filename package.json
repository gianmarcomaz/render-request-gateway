{
  "name": "render-request-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare backend for Wayframe render request ingestion.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types src/worker-configuration.d.ts --include-runtime false"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.18",
    "@cloudflare/workers-types": "latest",
    "@types/node": "^20.19.9",
    "typescript": "^5.8.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.20.5"
  }
}
